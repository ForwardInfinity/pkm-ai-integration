export const siteConfig = {
  name: "Refinery",
  description: "A selection environment for ideas. Knowledge grows through conjecture and refutation.",
  url: process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000",
} as const;
